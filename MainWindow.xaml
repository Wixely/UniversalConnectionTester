<Window x:Class="UniversalConnectionTester.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversalConnectionTester"
        mc:Ignorable="d"
        Title="Universal Connection Tester" Height="450" Width="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Endpoints from endpoints.json"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0 0 0 12"/>

        <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12">
                <StackPanel x:Name="EndpointButtonsPanel" />
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
